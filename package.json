{
  "name": "gentle-local-models",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi coding-agent extension that registers local OpenAI-compatible model servers as Pi providers.",
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "scripts": {
    "test": "npx vitest run"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "@types/node": "^26.4.0",
    "typebox": "1.3.7",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
